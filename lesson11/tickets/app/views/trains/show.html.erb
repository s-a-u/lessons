<p id="notice"><%= notice %></p>

<p>
  <strong>Number:</strong>
  <%= @train.number %>
</p>

<p>
  <strong>At Route :</strong>
  <%= @train.route.title if @train.route.present?%>
  <% if @train.route.present? %>

  <% else %>
  unknown_route
      <% end %>
</p>

<p>
  <strong>Car :</strong>
  <%= @train.car_number %>
  <% if @train.car_ids.present? %>
      <%= @train.car_number %>
  <% else %>
      unknown_car_type
  <% end %>
</p>

<p>
<strong>Ticket :</strong>
<% if @train.ticket_id.present? %>
    <%= @train.ticket_id %>
<% else %>
    unknown_ticket
<% end %>
</p>

<%= link_to 'Edit', edit_train_path(@train) %> |
<%= link_to 'Back', trains_path %>
