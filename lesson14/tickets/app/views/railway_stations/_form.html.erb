<%= form_for(railway_station) do |f| %>
    <%= render 'common/errors', resourse: @railway_station %>

  <div class="field">
    <%= f.label :title %>
    <%= f.text_field :title %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
