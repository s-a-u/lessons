<p id="notice"><%= notice %></p>

<p>
  <strong>Number:</strong>
  <%= @train.number %>
</p>

<p>
  <srtong>At Route :</srtong>
  <% if @train.current_route.present? %>
  <%= @train.current_route.title %>
      <% else %>
  unknown_route
      <% end %>
</p>

<p>
  <srtong>Car :</srtong>
  <% if @train.cars.present? %>
      <%= @train.cars.car_type %>
  <% else %>
      unknown_car_type
  <% end %>
</p>

<%= link_to 'Edit', edit_train_path(@train) %> |
<%= link_to 'Back', trains_path %>
